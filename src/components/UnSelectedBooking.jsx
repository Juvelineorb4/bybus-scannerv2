import { StyleSheet, Text, View } from "react-native";
import React from "react";

const UnselectedBooking = () => {
  return (
    <View style={{ flex: 1 }}>
      <Text style={{ textAlign: "center", fontFamily: 'bold' }}>Selecciona un viaje </Text>
    </View>
  );
};

export default UnselectedBooking;
